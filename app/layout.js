import "./globals.css";

export const metadata = {
  title: "Coach Connect",
  description: "Generated by coach connect app",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
